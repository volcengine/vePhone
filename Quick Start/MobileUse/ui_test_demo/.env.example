# 必填环境变量
VOLC_ACCESSKEY=
VOLC_SECRETKEY=
VOLC_HOST=
PRODUCT_ID=
POD_ID_LIST=
TOS_BUCKET=
TOS_ENDPOINT=
TOS_REGION=

# Optional runner config
# CASE_TIMEOUT_S=600 # 用例执行超时时间，默认600s
# POLL_INTERVAL_S=2 # 轮询间隔，默认2s

# Optional: 使用 src/video_prompt/ 通过视频生成Prompt
# 如果需要通过视频生成Prompt，以下环境变量必填
ARK_BASE_URL=
ARK_API_KEY=
ARK_MODEL_ID=
